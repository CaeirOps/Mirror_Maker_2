{
  "dashboard": {
    "id": null,
    "uid": null,
    "title": "Kafka Exporter Dashboard",
    "panels": [
      {
        "type": "stat",
        "title": "Total Topics",
        "targets": [
          {
            "expr": "sum(kafka_topic_count)",
            "format": "time_series"
          }
        ]
      },
      {
        "type": "stat",
        "title": "Total Partitions",
        "targets": [
          {
            "expr": "sum(kafka_partition_count)",
            "format": "time_series"
          }
        ]
      },
      {
        "type": "stat",
        "title": "Consumer Group Lag",
        "targets": [
          {
            "expr": "sum(kafka_consumergroup_group_lag) by (group)",
            "format": "time_series"
          }
        ]
      },
      {
        "type": "graph",
        "title": "Messages In per Second",
        "targets": [
          {
            "expr": "rate(kafka_topic_partition_current_offset[1m])",
            "format": "time_series"
          }
        ]
      },
      {
        "type": "stat",
        "title": "Total Consumers per Group",
        "targets": [
          {
            "expr": "count(kafka_consumergroup_current_offset)",
            "format": "time_series"
          }
        ]
      },
      {
        "type": "stat",
        "title": "Broker Count",
        "targets": [
          {
            "expr": "kafka_broker_count",
            "format": "time_series"
          }
        ]
      }
    ],
    "templating": {
      "list": []
    },
    "annotations": {
      "list": []
    },
    "refresh": "10s",
    "schemaVersion": 26,
    "version": 1,
    "links": []
  }
}